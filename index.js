const userName = document.getElementById("username");
const publication = document.getElementById("description");

const submitBtn = document.getElementById("publishButton");

submitBtn.addEventListener("click", (e, ev) =>
{
    alert("hello world");
});